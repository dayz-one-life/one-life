node_modules/
dist/
.turbo/
*.log
.env
.env.*
!.env.example
coverage/
scratchpad/
.superpowers/
docker-compose.override.yml

# generated media (hero images) — dev/prod data, never committed
data/

# harness
.claude/settings.local.json

# DayZ server logs dropped at repo root for analysis (package test fixtures still track)
/*.ADM
/*.RPT

# production DB dumps dropped at repo root for analysis — real player data, never committed
/*.sql
/*.dump
.next/

# OS / editor cruft
.DS_Store
**/.DS_Store

# >>> stow:base >>>
# managed by stow — edits inside this block are overwritten; put custom entries outside it
.DS_Store
Thumbs.db
# <<< stow:base <<<

# >>> stow:node >>>
# managed by stow — edits inside this block are overwritten; put custom entries outside it
node_modules/
npm-debug.log*
dist/
coverage/
.env
# <<< stow:node <<<
